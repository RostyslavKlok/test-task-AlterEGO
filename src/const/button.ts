export enum ButtonType {
  button = "button",
  submit = "submit",
}

export enum ButtonVariantType {
  contained = "contained",
  outlined = "outlined",
  text = "text",
}

export enum ButtonColorType {
  primary = "primary",
  contained = "contained",
  outlined = "outlined",
  text = "text",
}
